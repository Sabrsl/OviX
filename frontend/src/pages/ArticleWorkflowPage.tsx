/**
 * ArticleWorkflowPage - Page for article-by-article workflow
 * Uses the ArticleWorkflow component to display status cards and history
 */

import { ArticleWorkflow } from '../components/ArticleWorkflow'

export default function ArticleWorkflowPage() {
  return <ArticleWorkflow />
}
