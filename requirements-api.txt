# OVIX Backend API - Minimal Dependencies
# This file contains only the dependencies needed for the FastAPI backend
# Install with: pip install -r requirements-api.txt

# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-dotenv==1.0.0
pyyaml==6.0.1

# Wikipedia dependencies (minimal)
pywikibot==9.0.0
requests>=2.31.0

# Data processing
pandas>=2.0.0

# HTML parsing
beautifulsoup4>=4.12.0
