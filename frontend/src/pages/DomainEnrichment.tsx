/**
 * Domain Enrichment Page
 * 
 * Main page for enriching domain_to_site_name mappings from Wikidata.
 * Provides UI for category-based enrichment with sequential writes and robust error handling.
 */

import DomainEnrichmentSection from '../components/DomainEnrichmentSection'

export default function DomainEnrichment() {
  return <DomainEnrichmentSection />
}
